services:
  - type: web
    name: esp32-tablet-server
    runtime: docker
    dockerfilePath: ./server/Dockerfile
    dockerContext: ./server
    plan: free
    envVars:
      - key: PORT
        value: 5000
      - key: ESP32_IP
        value: 192.168.4.1
